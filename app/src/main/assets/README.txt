Assets layout:

1. `pose_landmarker_lite.task`
   MediaPipe Pose Landmarker model used by the training screen.

2. `source_images/`
   Raw source image slices kept for design handoff and future asset replacement.

Runtime UI image resources are still loaded from `app/src/main/res/drawable-nodpi/`.
