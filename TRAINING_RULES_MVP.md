# Gymmate 训练判定规则 MVP

本文档定义 Gymmate 当前 MVP 阶段的本地动作判定标准，目标是统一：

- 什么叫 `开始`
- 什么叫 `做到位`
- 什么叫 `算 1 次`
- 什么叫 `不计数`
- 什么叫 `错误但可计数`

当前版本采用：

- `MediaPipe Pose Landmarker` 提供关键点
- `本地规则引擎` 负责状态机、阈值、错误标签和计数
- `Android 客户端本地执行`

## 1. 通用判定合同

所有动作都遵循以下统一合同。

### 1.1 入镜门槛

只有关键点可见度达到门槛，动作才进入可判定状态。

- 上肢动作：肩、肘、腕必须稳定入镜
- 下肢动作：髋、膝、踝必须稳定入镜
- 建议门槛：平均可见度 `>= 0.58`

### 1.2 计数原则

Gymmate 不按“动了一下”计数，而按 `完整循环` 计数。

统一原则：

- 到达有效终点 A
- 再到达有效终点 B
- 再回到 A 或完成闭环
- 连续稳定 `4 到 6 帧`

只有完成闭环，才记 `1 次`

### 1.3 三种结果

每次动作只会落在以下三种结果之一：

1. `合格计数`
完整 ROM，完成循环，无硬失败

2. `带瑕疵计数`
完整 ROM，但存在软错误

3. `不计数`
未完成 ROM、未完成闭环、关键点丢失，或触发硬失败

### 1.4 硬失败 vs 软错误

硬失败：

- 不计数
- 直接提示“未完成”

软错误：

- 仍可计数
- 降分并输出纠错提示

## 2. 动作级规则

## 2.1 杠铃深蹲

推荐机位：`侧前 45°`

关键点：

- left_hip / right_hip
- left_knee / right_knee
- left_ankle / right_ankle
- left_shoulder / right_shoulder

状态定义：

- `起点`：膝角 `155° 到 175°`
- `底部`：膝角 `95° 到 110°`
- `完成`：站立 -> 底部 -> 重新站直

算 1 次：

- 到达底部
- 回到站立
- 站立稳定 `>= 4` 帧

不计数：

- 只蹲半程
- 到底后没站直
- 关键点缺失

硬失败：

- 深度不足

软错误：

- 膝内扣
- 躯干前倾过多
- 左右偏移

用户提示重点：

- 膝盖方向跟脚尖一致
- 下蹲到底再起身
- 起身时把胸口带起来

## 2.2 哑铃推肩

推荐机位：`正前`

关键点：

- left_shoulder / right_shoulder
- left_elbow / right_elbow
- left_wrist / right_wrist
- left_ear / right_ear

状态定义：

- `起点`：手腕在肩附近，肘屈曲
- `顶点`：手腕过耳线，肘接近伸直 `>= 155°`
- `完成`：肩旁准备位 -> 头顶推起 -> 回肩旁

算 1 次：

- 先推到顶
- 再回到肩旁准备位

不计数：

- 只推到额头附近
- 没回到肩旁

硬失败：

- 推举高度不足

软错误：

- 左右不稳定
- 手臂路径前飘
- 轻微代偿

用户提示重点：

- 往头顶正上方推
- 顶峰伸长手臂
- 两侧速度保持一致

## 2.3 哑铃侧平举

推荐机位：`正前`

关键点：

- left_shoulder / right_shoulder
- left_elbow / right_elbow
- left_wrist / right_wrist
- left_hip / right_hip

状态定义：

- `起点`：手落髋侧
- `顶点`：大臂外展到肩高附近
- `完成`：髋侧 -> 肩高 -> 回髋侧

算 1 次：

- 抬到肩高附近
- 回到髋侧

不计数：

- 最高点明显不足

硬失败：

- 抬手高度不足

软错误：

- 左右不对称
- 借力摆动
- 耸肩代偿

用户提示重点：

- 抬到肩高就够
- 不要靠甩起来
- 两边终点尽量一致

## 2.4 坐姿绳索划船

推荐机位：`前方 45°`

关键点：

- left_shoulder / right_shoulder
- left_elbow / right_elbow
- left_wrist / right_wrist

状态定义：

- `起点`：手臂前伸
- `终点`：肘明显过躯干，肩向后收
- `完成`：前伸 -> 回拉 -> 回前伸

算 1 次：

- 肘回拉到位
- 再回到前伸位

不计数：

- 肘没明显回到躯干后方
- 回程不完整

硬失败：

- 拉肘不充分

软错误：

- 左右不对称
- 回拉不紧
- 耸肩代偿

用户提示重点：

- 先收肩胛再拉肘
- 手柄拉近身体
- 慢速回程

## 2.5 高位下拉

推荐机位：`前方 45°`

关键点：

- left_shoulder / right_shoulder
- left_elbow / right_elbow
- left_wrist / right_wrist

状态定义：

- `起点`：手臂完全上举
- `终点`：横杆到上胸附近，肘向下向后
- `完成`：完全伸展 -> 下拉 -> 回完全伸展

算 1 次：

- 下拉到底
- 再回到完全伸展

不计数：

- 只拉半程
- 没放回顶端

硬失败：

- 下拉不充分

软错误：

- 肘部收得不够
- 左右发力不一致

用户提示重点：

- 再拉低一点
- 底部肘往下往后
- 顶端要完整还原

## 2.6 杠铃卧推 / 器械推胸

推荐机位：`前方 45°`

关键点：

- left_shoulder / right_shoulder
- left_elbow / right_elbow
- left_wrist / right_wrist

状态定义：

- `底部`：肘明显屈曲，腕接近胸线
- `顶部`：肘接近伸直 `>= 155°`
- `完成`：底部 -> 顶部 -> 底部，或顶部 -> 底部 -> 顶部

算 1 次：

- 到达另一终点
- 再回到原终点

不计数：

- 只推半程
- 顶部未到位
- 底部未到位

硬失败：

- ROM 不完整

软错误：

- 肘外展过多
- 左右路径不稳

用户提示重点：

- 顶部推直
- 回到底部
- 肘别散太开

## 2.7 器械腿屈伸

推荐机位：`侧前 45°`

关键点：

- left_hip / right_hip
- left_knee / right_knee
- left_ankle / right_ankle

状态定义：

- `起点`：屈膝位
- `顶点`：膝接近伸直 `>= 160°`
- `完成`：屈膝 -> 伸直到顶 -> 回屈膝

算 1 次：

- 伸直到位
- 再回到底

不计数：

- 没伸直到位
- 没回到底

硬失败：

- 伸膝不充分

软错误：

- 双腿节奏不一致
- 顶峰控制不足

用户提示重点：

- 顶峰伸直再停一下
- 双腿同速

## 3. 当前代码映射建议

建议后续把每个动作抽象成统一配置：

```kotlin
data class ActionRuleProfile(
    val name: String,
    val cameraView: String,
    val requiredLandmarks: List<String>,
    val startRule: String,
    val endRule: String,
    val countCycle: String,
    val hardFails: List<String>,
    val softWarnings: List<String>,
)
```

当前项目已具备初始动作级 detector：

- 杠铃深蹲
- 哑铃推肩
- 哑铃侧平举
- 坐姿绳索划船

后续应继续补全：

- 高位下拉
- 杠铃卧推
- 器械推胸
- 器械腿屈伸

## 4. MVP 实施优先级

第一优先级：

- 规则闭环正确
- 不乱计数
- 主错误提示明确

第二优先级：

- 分数更细
- 左右对称更稳定
- 提示文案更自然

第三优先级：

- 后台动态下发阈值
- 按用户身高、机位、训练水平做阈值自适应
