# Gymmate MVP PRD（Android 原生 Kotlin 版）

## 1. 文档定位

本 PRD 基于你提供的《MVP功能模块》整理，目标是把 Gymmate 第一版做成一个可真正开工的移动端产品方案。

约束条件如下：

- 仅做移动端
- 仅做 Android
- 客户端使用原生 Kotlin 实现
- 姿态识别固定使用 MediaPipe
- 后端优先走极简轻量方案
- MVP 必须围绕 4 个核心模块展开
- 优先保证“能跑通闭环”，而不是一开始追求最强 AI

本文档同时吸收两类公开参考：

1. 产品结构参考：`RepDetect`
2. 姿态识别接入参考：Google 官方 `MediaPipe Pose Landmarker Android sample`

参考链接：

- RepDetect: https://github.com/giaongo/RepDetect
- MediaPipe Pose Landmarker Android: https://ai.google.dev/edge/mediapipe/solutions/vision/pose_landmarker/android
- MediaPipe Samples: https://github.com/google-ai-edge/mediapipe-samples
- SQLite 使用场景: https://www.sqlite.org/whentouse.html
- SQLite FTS5: https://www.sqlite.org/fts5.html

---

## 2. 产品一句话

Gymmate 是一款面向健身初学者和轻度进阶用户的 Android 健身训练助手，核心价值是通过摄像头姿态识别、规则判断和 AI 文本反馈，帮助用户完成基础动作纠错、记录训练结果，并给出基础训练计划。

---

## 3. 产品目标

MVP 的目标不是做一个“全能 AI 私教”，而是先验证下面这条最小闭环是否成立：

1. 用户打开 App
2. 选择动作并开始训练
3. App 通过摄像头识别动作
4. 系统实时或训练后判断动作质量、次数和错误类型
5. App 生成能看懂的反馈
6. 系统保存训练记录和趋势
7. 用户能继续跟着基础训练模板训练

如果这条链路成立，MVP 就具备继续迭代为 AI 健身产品的基础。

---

## 4. MVP 必做范围

MVP 只做以下 4 个核心模块：

1. AI 动作纠错
2. 精简动作库
3. 训练记录与反馈
4. 基础训练体系

MVP 必须包含以下基础系统能力：

1. Android 前端
2. 后端服务
3. 姿态识别模块
4. 规则判断模块
5. LLM 调用模块
6. 动作库内容模块
7. 数据库
8. Prompt 模板
9. 用户认证与训练记录模块
10. 训练计划模板模块

---

## 5. 用户画像

优先服务两类用户：

1. 健身新手
特点：不知道动作是否标准，缺少反馈，训练计划混乱。

2. 有基础但缺少指导的用户
特点：会去健身房，但动作质量不稳定，希望获得低成本、即时的动作反馈。

MVP 暂不服务：

- 专业健美用户
- 强依赖复杂周期训练的深度用户
- 需要高精度医疗/康复级动作分析的用户

---

## 6. 核心价值主张

### 6.1 用户价值

- 训练时知道自己哪里做错了
- 训练后知道自己今天练得怎么样
- 能持续看到进步趋势
- 不需要自己拼凑训练计划

### 6.2 产品差异点

- 不是单纯动作演示库，而是“识别 + 判断 + 反馈 + 记录”闭环
- DeepSeek 不做裁判，只做“表达层”和“教练化反馈层”
- MVP 先聚焦 10 到 20 个基础动作，保证动作判断质量

---

## 7. 设计方向

这部分参考 `frontend-design` skill 的要求，先把产品视觉和交互基调定下来，避免后期 UI 风格飘。

### 7.1 Purpose

这个 App 的核心任务是让用户在训练过程中获得“可靠、明确、像教练一样”的反馈。

### 7.2 Tone

建议风格关键词：`专业`、`克制`、`运动感`、`有判断力`

不要做成：

- 泛娱乐风
- 花哨健身打卡风
- 过于医疗化的冷感风

### 7.3 Differentiation

让用户记住 Gymmate 的关键不是“页面好看”，而是：

- 打开训练页就很专注
- 实时反馈简单直接
- 错误提示像一个懂训练的教练在说话

### 7.4 Android UI 方向

- 采用 Material 3 作为基础设计系统
- 页面以深色训练场景为主，搭配高对比状态色
- 训练页优先突出摄像头、骨架、次数、错误提示、结束评分
- 避免信息堆叠，训练场景每屏只保留关键任务

---

## 8. 功能模块拆解

## 8.1 模块一：AI 动作纠错

这是 MVP 的第一核心模块，也是最重要的技术壁垒。

### 功能目标

用户在训练时，系统能识别动作过程，并输出：

- 当前动作是否有效
- 当前完成了多少次
- 是否出现常见错误
- 本组训练评分
- 训练结束后的纠错建议

### 必做子模块

1. 摄像头训练页
2. 姿态识别
3. 规则判断
4. 次数统计
5. 错误标签系统
6. 评分逻辑
7. 反馈生成
8. 结果展示页

### 页面要求

训练页至少包含：

- 相机预览
- 骨架覆盖层
- 当前动作名称
- 次数统计
- 当前状态提示
- 错误提示
- 开始/暂停/结束按钮

结果页至少包含：

- 本次训练动作
- 完成组数/次数
- 平均评分
- 高频错误标签
- 教练式总结
- 下次注意点

### 核心实现逻辑

#### A. 姿态识别

职责：

- 从摄像头帧中提取人体关键点
- 获取肩、肘、髋、膝、踝等坐标
- 输出给规则模块

#### B. 规则判断

职责：

- 判断当前动作阶段
- 判断动作是否达标
- 判断是否出现错误动作
- 统计一次完整动作
- 输出动作评分

注意：

- 这部分必须由规则引擎承担
- 不交给 LLM 判断动作对错

#### C. LLM 表达层

职责：

- 把错误标签翻译为人话
- 生成训练结束后的简短建议
- 控制反馈语气统一

不承担：

- 动作识别
- 次数统计
- 评分判定

### MVP 推荐先支持的动作

建议第一版控制在 8 到 12 个动作，优先选择姿态规则相对明确的动作，避免一开始就做过难动作。

优先级建议：

1. 杠铃深蹲
2. 哑铃推肩
3. 哑铃侧平举
4. 高位下拉
5. 坐姿绳索划船
6. 杠铃卧推
7. 器械推胸
8. 器械腿屈伸

第二批再扩展：

- 引体向上
- 俯身杠铃划船
- 直杆绳索弯举
- 直杆绳索下压

原因：

- 第一批动作更适合建立姿态识别与规则模板
- 部分动作器械路径更稳定，误判更容易控制

---

## 8.2 模块二：精简动作库

这是内容底座，也是规则系统的数据来源。

### 功能目标

用户可以查看每个动作的标准做法、目标肌群、常见错误和训练要点，为训练页和计划页提供统一内容基础。

### 必做内容结构

每个动作必须包含：

- 动作名
- 分类
- 目标肌群
- 动作简介
- 标准动作说明
- 常见错误
- 纠正建议
- 视频/GIF/封面图
- 判定规则参数
- 动作适用等级

### 页面要求

动作库页：

- 动作列表
- 分类筛选
- 搜索框
- 动作卡片

动作详情页：

- 动作介绍
- 目标肌群
- 标准动作步骤
- 常见错误
- 纠错提示
- 演示媒体
- 开始训练入口

### MVP 内容策略

- 第一版动作数量控制在 10 到 20 个
- 每个动作必须有统一结构化字段
- 每个动作都要绑定对应规则模板

---

## 8.3 模块三：训练记录与反馈

这是 MVP 的留存基础。

### 功能目标

帮助用户回看自己过去练了什么、练得怎么样、哪类错误最常见、有没有进步。

### 必做能力

1. 训练记录保存
2. 动作结果保存
3. 错误标签保存
4. 训练历史页
5. 简单趋势图
6. 打卡逻辑

### 页面要求

历史页至少展示：

- 训练日期
- 完成的动作
- 总次数
- 平均评分
- 高频错误

趋势页至少展示：

- 最近 7 天或 30 天训练次数
- 平均评分变化
- 错误减少趋势

### MVP 不必做

- 社区排行
- 复杂勋章体系
- 分享海报

---

## 8.4 模块四：基础训练体系

这是产品专业感的来源，但 MVP 不做过深个性化。

### 功能目标

让用户根据训练目标快速拿到基础训练模板，而不是从零开始自己排计划。

### 必做能力

1. 目标选择
2. 模板计划展示
3. 模板计划详情
4. 用户选择计划
5. 训练计划落库
6. 简单计划说明生成

### MVP 方案

第一版不做完全自由生成，只做“固定模板 + 轻微个性化”。

建议做 3 类基础目标：

1. 增肌入门
2. 减脂塑形
3. 基础力量提升

每类目标提供 1 到 2 套固定模板。

DeepSeek 在这里的作用：

- 润色计划说明
- 根据用户训练频次、目标、基础信息做轻微文案调整

不做：

- 从零自动生成专业周期计划
- 动态按疲劳度实时改计划

---

## 9. MVP 用户流程

### 9.1 首次使用流程

1. 用户注册/登录
2. 填写基础信息
3. 选择训练目标
4. 系统推荐基础模板
5. 用户进入首页

### 9.2 训练流程

1. 用户进入动作库或计划页
2. 选择动作
3. 打开训练页
4. 允许摄像头权限
5. 开始训练
6. 系统实时识别并统计
7. 用户结束本组/本次训练
8. 系统生成评分与反馈
9. 系统保存记录

### 9.3 回看流程

1. 用户进入历史页
2. 查看训练记录
3. 查看动作评分趋势
4. 查看高频错误
5. 根据建议继续下一次训练

---

## 10. Android 原生 Kotlin 实现方案

## 10.1 客户端推荐架构

推荐使用：

- Kotlin
- Jetpack Compose
- MVVM
- Clean Architecture
- 单 Activity + Navigation Compose

推荐分层：

1. `app`
2. `core`
3. `feature-auth`
4. `feature-training`
5. `feature-exercise`
6. `feature-plan`
7. `feature-history`
8. `data`
9. `domain`

### 分层职责

`presentation`：

- 页面 UI
- 状态管理
- 用户交互

`domain`：

- 用例
- 实体
- 业务规则接口

`data`：

- 网络请求
- 本地缓存
- Repository 实现

### 为什么这样做

因为 Gymmate 后续一定会继续扩展动作、计划、AI 反馈、设备能力，如果一开始把逻辑全堆在页面层，后面会很难维护。

---

## 10.2 Android 关键能力选型

### UI

- Jetpack Compose
- Material 3

理由：

- 开发效率高
- 更适合快速迭代 MVP
- 更容易做训练页状态控制

### 相机

- CameraX

理由：

- Android 官方方案
- 与生命周期集成较好
- 容易做预览、帧分析、权限管理

### 本地数据

- Room
- DataStore

用途：

- Room 存训练缓存、动作缓存、草稿状态
- DataStore 存 token、用户偏好、训练设置

### 网络

- Retrofit
- OkHttp
- Kotlinx Serialization

### 依赖注入

- Hilt

### 并发

- Kotlin Coroutines
- Flow

### 图表

- Vico 或 MPAndroidChart

MVP 建议优先选 `Vico`，更适合 Compose。

---

## 11. 开源参考项目与借鉴策略

## 11.1 最值得参考的开源项目

如果目标是“极简、能直接借鉴、尽快做出 Android MVP”，当前最值得参考的开源母本建议定为：

- `RepDetect`

适合作为母本的原因：

- 它本身就是 Android Kotlin 健身训练 App
- 已经包含训练、计数、历史、计划等产品形态
- 比纯姿态 demo 更接近你要做的产品闭环
- 可以直接借鉴它的页面流、训练流、历史结构和本地存储思路

但它不能整仓照搬，原因也要明确：

- 它的姿态识别思路并不完全等于你当前锁定的 MediaPipe
- 它带有更复杂的训练/分类器思路，不够“极简”
- 你现在要的是“规则引擎 + MediaPipe”，而不是“先训练一个动作分类模型”

所以最合理的策略不是“抄一个仓库”，而是“拆成两部分借鉴”。

## 11.2 推荐借鉴方式

### 借鉴 `RepDetect` 的部分

- 页面结构
- 训练流程
- 历史记录结构
- 本地数据落盘思路
- 动作计划与动作库组织方式

### 借鉴 Google 官方 sample 的部分

- CameraX 接入方式
- `PoseLandmarkerHelper` 结构
- landmark 输出与 overlay 绘制
- 帧分析管线
- MediaPipe Tasks 依赖与初始化方式

## 11.3 为什么不继续找“更复杂、更强”的开源项目

因为你当前阶段最重要的不是找到一个“论文级精度”的仓库，而是找到一个：

1. 能快速改成 Gymmate 的壳子
2. 能支撑 MediaPipe 本地识别
3. 能尽快打通训练闭环
4. 不会因为后端和模型过重拖慢 MVP

这个标准下，`RepDetect + 官方 MediaPipe sample` 的组合比单独寻找一个“大而全”的项目更适合你。

---

## 12. 姿态识别技术栈建议

这是整个 MVP 最关键的技术选型。

## 12.1 最终技术决策

第一版固定使用：

- Android 端本地跑 `MediaPipe Pose Landmarker`
- 规则判断放客户端本地执行
- LLM 仅调用服务端

这意味着第一版不再比较 `MoveNet`、`RTMPose`、`MMPose` 等方案，直接收敛到 `MediaPipe`。

## 12.2 为什么固定 MediaPipe

- 官方 Android 支持成熟
- 有明确的 sample 和接入文档
- 适合单人实时姿态识别
- 对 MVP 来说工程复杂度更低
- 更适合“先做规则判断，再做产品验证”的路线

## 12.3 MVP 实施方式

建议直接采用 Google 官方 Pose Landmarker Android 接入方式：

- `CameraX` 获取预览帧
- `MediaPipe Tasks Vision` 做 landmark 推理
- `OverlayView` 绘制骨架
- landmark 输出交给本地规则引擎

## 12.4 当前不采用的姿态路线

MVP 第一版明确不采用：

- MoveNet
- RTMPose
- MMPose
- 云端视频识别
- 自训练动作分类模型

原因：

- 会明显增加集成和调优成本
- 不符合“极简 MVP”目标
- 会让团队注意力从产品闭环转向模型实验

---

## 13. 规则判断模块怎么实现

规则模块是 Gymmate 的核心业务逻辑，不应该混在 UI 层，也不应该交给 LLM。

## 13.1 输入

输入来自姿态识别模块：

- 每帧关键点坐标
- 关键点置信度
- 时间序列

## 13.2 输出

- 当前动作阶段
- 是否完成一次有效重复
- 错误标签列表
- 实时提示
- 单次评分
- 本组总评分

## 13.3 推荐实现方式

第一版采用：

- `动作模板 + 状态机 + 阈值规则`

### 具体做法

每个动作定义：

1. 关键检测关节
2. 起始姿态条件
3. 离心/向下阶段条件
4. 向心/向上阶段条件
5. 完成一次动作的闭环条件
6. 常见错误判定条件
7. 各错误扣分权重

### 示例：深蹲

可用规则包括：

- 髋部高度变化
- 膝关节角度变化
- 躯干前倾角
- 膝盖轨迹是否内扣
- 底部深度是否达标

### 示例：侧平举

可用规则包括：

- 肘部相对肩部高度
- 躯干摆动幅度
- 左右侧对称性
- 顶峰停留稳定性

## 13.4 规则模块架构建议

建议做成可配置结构，而不是把规则全写死在代码里。

推荐结构：

- `ExerciseRuleDefinition`
- `RepCounterEngine`
- `FormErrorDetector`
- `ScoreCalculator`

规则参数可来自后端动作库配置。

这样后续迭代新动作时，不需要每次都重写大段客户端逻辑。

---

## 14. LLM 模块怎么实现

## 14.1 LLM 定位

LLM 在 Gymmate MVP 中只负责：

1. 把结构化错误标签转成自然语言反馈
2. 生成训练结束总结
3. 生成基础训练计划说明
4. 回答有限范围的基础健身问答

## 14.2 不让 LLM 做的事

- 直接看视频判断动作
- 决定次数统计
- 决定动作是否标准
- 自行编造训练建议

## 14.3 服务端调用方式

推荐：

- Android 不直接调用 DeepSeek
- 由后端统一代理调用 LLM

原因：

- 保护 API Key
- 统一 Prompt 管理
- 统一输出格式
- 可做审计、降级、缓存

## 14.4 Prompt 设计

Prompt 必须模板化，至少约束：

- 输出语气
- 输出长度
- 输出结构
- 禁止编造医学/康复建议
- 禁止超出错误标签自由发挥

推荐返回结构：

- `summary`
- `top_errors`
- `next_time_focus`
- `coach_tip`

---

## 15. 后端技术栈建议

MVP 后端目标是：

- 快速开发
- 稳定接入数据库
- 易于管理动作库和计划模板
- 易于代理 LLM 请求

## 15.1 最终推荐方案

如果你的目标是“极简、轻量、单人或小团队快速验证 MVP”，后端推荐直接定为：

- `FastAPI + SQLite`

这是比 `NestJS + PostgreSQL` 更轻的一条路线，更适合你当前阶段。

## 15.2 为什么不再优先用更重的后端栈

不优先选择较重栈的原因：

- 当前数据量很小
- 管理后台需求很轻
- 搜索需求很轻
- 不需要一开始就为扩容和复杂治理付出代价
- 训练主逻辑在端上，服务端更多是“存储 + 配置 + LLM 代理”

## 15.3 SQLite 是否可行

SQLite 在当前 MVP 阶段是可行的，但要明确适用边界。

适合的前提：

- 后端单实例部署
- 不把原始视频持续上传入库
- 不把每帧 landmark 明细都高频写库
- 服务端主要保存训练结果摘要、动作库、计划、用户信息和反馈结果

为什么可行：

- Gymmate 的 MVP 不是高并发系统
- 动作库只有 10 到 20 个
- 搜索规模很小
- 训练中主要计算发生在 Android 本地
- 服务端写入频率不会特别高

什么时候不该继续用 SQLite：

- 需要多实例横向扩容
- 需要复杂后台和多人高频编辑
- 需要复杂报表统计
- 需要大量异步任务并发写库
- 需要把训练过程原始数据大规模留存

## 15.4 SQLite 使用建议

如果采用 SQLite，建议这样用：

- 开启 `WAL`
- 使用短事务
- 做定时备份
- 为常查字段建立索引
- 训练过程只上传“结果”，不上传“全量过程数据”

## 15.5 为什么我推荐 FastAPI

- 学习和实现成本低
- 很适合做轻量 API 服务
- 适合快速代理 DeepSeek
- 配合 SQLite 开发成本低
- 后期如果迁移到 PostgreSQL，成本也可控

---

## 16. 搜索能力用什么技术栈

如果你这里说的“搜索”是 App 内动作库搜索、计划搜索、内容检索，那么在极简方案下，MVP 应该直接使用 SQLite 自带能力。

## 16.1 MVP 最终推荐

- `SQLite + LIKE`
- `SQLite FTS5`

适用场景：

- 动作名搜索
- 肌群标签搜索
- 分类筛选
- 计划模板搜索

### 原因

- MVP 动作库只有 10 到 20 个动作，数据量极小
- 没必要引入 Elasticsearch
- 没必要为了搜索单独上 PostgreSQL
- SQLite 的全文检索对当前规模完全够用

## 16.2 推荐落地方式

第一版：

- 动作名用 `LIKE`
- 分类、肌群用普通索引筛选

第一版增强：

- 动作描述
- 常见错误
- 纠正建议
- 计划说明

这些文本字段可接入 `FTS5`。

## 16.3 如果后期扩展内容生态

当你后面开始做：

- 大量动作库
- 健身问答内容库
- 训练知识文章
- 多标签复杂检索

再升级为：

- `Meilisearch`

或：

- `Elasticsearch`

### 推荐顺序

1. MVP：SQLite + LIKE
2. MVP 增强：SQLite FTS5
3. 成长期：Meilisearch
4. 大规模内容检索：Elasticsearch

## 16.4 如果你说的是“AI 搜索”

如果后面要做的是“训练知识问答”或“动作教学问答”的 AI 搜索，不是当前 MVP 第一优先级。

后续可以这样扩展：

- 向量化：bge-m3 / text-embedding 系列
- 向量库：pgvector
- 检索增强：RAG

但 MVP 第一版先不要上，避免复杂度失控。

---

## 17. 数据库设计建议

MVP 先用 SQLite。

## 17.1 核心表

### users

- id
- phone_or_email
- password_hash
- nickname
- gender
- height
- weight
- fitness_goal
- training_level
- created_at

### exercises

- id
- name
- category
- target_muscles
- cover_url
- media_url
- description
- standard_steps
- common_errors
- correction_tips
- difficulty_level
- is_active

### exercise_rule_configs

- id
- exercise_id
- pose_model_type
- rep_logic_config
- error_logic_config
- score_logic_config
- version
- updated_at

### training_plans

- id
- name
- goal_type
- level
- description
- schedule_json
- is_active

### user_training_plans

- id
- user_id
- training_plan_id
- start_date
- status

### workout_sessions

- id
- user_id
- exercise_id
- started_at
- ended_at
- total_reps
- avg_score
- summary_text

### workout_rep_results

- id
- session_id
- rep_index
- score
- error_tags
- phase_trace
- created_at

### workout_error_stats

- id
- session_id
- error_tag
- count

---

## 18. 接口设计建议

## 18.1 客户端 -> 服务端

### 认证相关

- `POST /auth/register`
- `POST /auth/login`
- `GET /me`

### 动作库

- `GET /exercises`
- `GET /exercises/{id}`

### 训练计划

- `GET /plans`
- `POST /user/plans/select`
- `GET /user/plans/current`

### 训练记录

- `POST /workout/sessions`
- `POST /workout/sessions/{id}/finish`
- `GET /workout/history`
- `GET /workout/history/{id}`

### AI 反馈

- `POST /ai/feedback`

输入：

- 动作名
- 错误标签
- 评分
- 用户基础信息

输出：

- 总结
- 重点错误
- 下次注意点
- 教练建议

---

## 19. 本地与服务端职责边界

为了保证 MVP 跑得顺，建议明确边界。

## 18.1 Android 本地负责

- 相机采集
- 姿态识别
- 实时骨架绘制
- 基础规则判断
- 次数统计
- 实时状态提示
- 训练中临时数据缓存

## 18.2 服务端负责

- 用户认证
- 动作库
- 计划模板
- 训练记录持久化
- LLM 代理
- Prompt 管理
- 后台规则配置下发

### 为什么这样切

因为实时反馈必须低延迟，本地做最稳。

而需要统一管理和沉淀的数据、LLM、配置，放服务端最合理。

---

## 20. 非功能要求

### 性能

- 训练页启动到可识别时间尽量控制在 3 秒内
- 实时识别尽量达到可接受帧率
- 低端 Android 机型也要保证基础可用

### 稳定性

- 相机权限被拒绝时有清晰兜底
- 姿态识别失败时有明确提示
- LLM 服务异常时不影响训练记录保存

### 安全

- API Key 不放客户端
- 用户隐私数据加密传输
- 尽量不默认上传原始视频

### 可运营性

- 动作库与规则参数可通过后台更新
- Prompt 可单独维护版本

---

## 21. MVP 不做什么

为了控制范围，第一版明确不做以下内容：

- iOS 版本
- Web 版本
- 多人同屏识别
- 云端视频分析
- 社区功能
- 私聊教练
- 完整 AI 问答知识库
- 复杂个性化训练周期
- 可穿戴设备联动
- 精细化饮食系统

---

## 22. 分阶段开发建议

## Phase 1：基础闭环

目标：

- 登录注册
- 动作库
- 训练页相机
- 本地姿态识别
- 2 到 3 个动作规则
- 次数统计
- 训练记录保存

## Phase 2：可用 MVP

目标：

- 动作扩展到 8 到 12 个
- 评分系统
- 错误标签系统
- 历史记录和趋势
- 训练计划模板
- DeepSeek 反馈总结

## Phase 3：可验证产品价值

目标：

- 动作库扩展到 15 到 20 个
- 规则参数化
- 后台管理动作与模板
- Prompt 版本管理
- 搜索与筛选优化

---

## 23. 当前最终推荐技术栈

### Android

- Kotlin
- Jetpack Compose
- CameraX
- Hilt
- Room
- DataStore
- Retrofit
- Coroutines + Flow

### AI / 姿态识别

- MediaPipe Pose Landmarker
- 本地规则引擎

### 后端

- FastAPI
- SQLite

### AI 服务

- DeepSeek API
- 服务端 Prompt 模板管理

### 搜索

- MVP：SQLite LIKE
- 增强：SQLite FTS5
- 后续扩展：Meilisearch

---

## 24. 最终结论

Gymmate 的 MVP 不应该理解成“做一个 AI 模型”，而应该理解成“做一个能跑通训练闭环的小系统”。

这个闭环里最关键的不是 LLM，而是三件事：

1. 本地姿态识别是否稳定
2. 规则判断是否可信
3. 反馈和记录是否让用户愿意持续使用

如果现在进入开发，我建议你的最优先工程路线是：

1. 先定 8 到 12 个首发动作
2. 先做 2 到 3 个动作的规则原型
3. Android 端先打通 CameraX + MediaPipe + 本地计数
4. 服务端先用 FastAPI + SQLite 搭起动作库、记录、LLM 反馈代理
5. 搜索先用 SQLite，不额外引入复杂搜索中间件

这会是当前阶段最稳、最像一个真正 MVP 的落地路径。
